services:
  form-agent:
    build: .
    env_file: .env
    environment:
      AGENT_DB_PATH: /data/agent.db
      SURVEY_PATH: /app/agent/survey.yaml
    volumes:
      - ./data:/data
    # TUI is interactive — start it with:  docker compose run --rm form-agent
    stdin_open: true
    tty: true
